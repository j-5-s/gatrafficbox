<!DOCTYPE html>
<html>
  <head>
    <title>Setup</title>
    
    <link rel='stylesheet' href='/vendor/bootstrap/css/bootstrap.min.css' />
    <link rel='stylesheet' href='/vendor/bootstrap/css/bootstrap-theme.min.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="/javascripts/jquery-1.10.2.min.js"></script>
    <script src="/vendor/bootstrap/js/bootstrap.min.js"></script>
    <script src="/javascripts/app.js"></script>
  
  </head>
  <body>

    
    <div class="container">
      <%- include partials/header.ejs %>
      <h1>Setup</h1>
      <form class="form" method="post" action="/setup">
        <fiedset><legend>Google API</legend>
        <% if (status === 'updated') { %>
          <p class="alert alert-success">Account informatino saved!</p>
        <% } %>
        <%- (typeof error !== 'undefined' && error.length) ? '<p class="alert alert-danger">'+error+'</p>' : '' %>
        <p>In order to access Google Analytics Real Time API, you need to provide API information for your Google Analytics Account.
        You will need to create a new project at the <a href="https://cloud.google.com/console‎">Google Analytics Cloud Console</a>
        and provide the below information for your project</p>
        <div class="form-group">
          <label for="client_id">Client ID</label>
          <input class="form-control" type="text" name="client_id" value="<%= setup.client_id || '' %>" />
        </div>
        <div class="form-group">
          <label for="client_secret">Client Secret</label>
          <input class="form-control" type="text" name="client_secret" value="<%= setup.client_secret || '' %>" />
        </div>
        <div class="form-group">
          <label for="redirect_url">Redirect URL</label>
          <input class="form-control" type="text" name="redirect_url" value="<%= setup.redirect_url || '' %>" />
        </div>
        

      </fieldset>
      <fieldset><legend>Spark API</legend>
        <div class="form-group">
          <label for="spark_id">Spark ID</label>
          <input class="form-control" type="text" name="spark_id" value="<%= setup.spark_id || '' %>" />
        </div>
        <div class="form-group">
          <label for="spark_key">Spark Key</label>
          <input class="form-control" type="text" name="spark_key" value="<%= setup.spark_key || '' %>" />
        </div>
      </fieldset>

      <fieldset><legend>Site Authentication</legend>
        <p>Password protection of this site is mandatory. Please create a username/password</p>
        <div class="form-group">
          <label for="user_name">User Name</label>
          <input class="form-control" type="text" name="user_name" value="<%= setup.user_name || 'admin' %>" />
        </div>
        
        <div class="form-group">
          <label for="password">Password</label>
          <input class="form-control" type="text" name="password" value="<%= setup.password || 'admin' %>" />
        </div>


      </fieldset>

      <fieldset><legend>Debugging</legend>
        <div class="form-group">
          <label for="debug">Debug Mode</label><br>
          <input type="radio" name="debug" value="on" <%= (setup.debug === 'on') ? 'checked="yes"' : '' %> /> On
          <input type="radio" name="debug" value="off" <%= (setup.debug !== 'on') ? 'checked="yes"' : '' %>  /> Off
          <p class="text-muted">It's recommended to leave off. If on, log files will be written to and accumulate data.</p>
        </div>
      </fieldset>

      <input type="submit" class="btn btn-primary" value="Update" />
      </form>
    </div><!--/container-->
  </body>
</html>